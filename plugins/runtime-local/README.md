# runtime-local

Default Union Street runtime. Agents run on the host with a local workspace directory and loopback HTTP ingress for MCP, Lash, webhooks, and control traffic.

Contract outputs: `compute_endpoint`, `storage_mount`, `ingress_url`, `control_url`.
