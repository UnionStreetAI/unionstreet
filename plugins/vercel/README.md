# Vercel Plugin

CLI guidance plugin for Vercel. This teaches agents how to use `vercel` for existing projects, deployments, env vars, logs, domains, and previews.

This is not the Union Street Vercel runtime/sandbox provider.
