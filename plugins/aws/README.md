# AWS Plugin

CLI guidance plugin for AWS. This teaches agents safe `aws` CLI basics for inspecting accounts, logs, storage, compute, and deployments.

This is not the Union Street AWS runtime provider.
